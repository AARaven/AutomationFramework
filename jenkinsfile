pipeline {

    agent any

    stages {

        stage ('Compilation stage') {

            steps {
                    bat 'mvn clean compile'
            }
        }

        stage ('Testing stage') {

            steps {
                    bat 'mvn clean test'
            }
        }
    }
}