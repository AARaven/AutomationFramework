pipeline {

    agent any

    stages {

        stage ('Compilation stage') {

            steps {
                    bat '%MAVEN_HOME%\bin\mvn clean compile'
            }
        }

        stage ('Testing stage') {

            steps {
                    bat '%MAVEN_HOME%\bin\mvn clean test'
            }
        }
    }
}