pipeline {

    agent any

    stages {

        stage ('Compilation stage') {

            steps {
                bat '%MAVEN_HOME%\bin'
                bat 'mvn clean compile'
            }
        }

        stage ('Testing stage') {

            steps {
                bat '%MAVEN_HOME%\bin'
                bat 'mvn clean test'
            }
        }
    }
}